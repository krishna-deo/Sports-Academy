# Project Rules

## Git & Deployment Rules
- NEVER push code changes to GitHub (`git push`) automatically.
- Only run `git push` when the user explicitly requests or instructs to push to GitHub.
- Allow the user to inspect and test all changes locally first.
